//Huffman code

#include <iostream>

using namespace std;

int main()
{
    char text[]={'f','e','c','b','d','a'};
    int count[]={5, 9, 12, 13, 16, 45};

    for(int i=0; i<6; i++)
    {
        if(count [i] + count [i+1])
        {
            g
        }
    }
    cout << "Hello world!" << endl;
    return 0;
}
